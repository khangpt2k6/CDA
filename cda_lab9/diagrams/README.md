# Lab 9 — quick block view (optional)

Lab~9’s “figure” is usually a **Vivado schematic screenshot** (`verilog_lab99.png`). You cannot recreate that faithfully in Mermaid without re-drawing the netlist by hand.

If you only need a **concept block** for slides or notes, see `fpga-counter.mmd`. For the real graded deliverable, keep using the Vivado export.
